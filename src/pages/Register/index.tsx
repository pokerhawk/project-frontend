import RegisterForm from '../../components/RegisterForm';
import { SignPageWrapper } from '../../styles/Global';

const Register = () => {
    return (
        <SignPageWrapper>
            <RegisterForm />
        </SignPageWrapper>
    );
}

export default Register;
